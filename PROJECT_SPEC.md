# Cuebook Rehearsal Lab 项目规格

## 项目目标

该项目为现场演出排练团队提供一致的 cue book 准备流程。它将跨部门的灯光、音响、舞台和投影提示组织为一次排练运行，并在发布前要求所有关键提示完成可追溯的复核。

## 用户角色

- 舞台监督：创建排练运行并确认节目名称和导演。
- 部门负责人：核对本部门提示，记录同意或需调整的结论。
- 场记：查看当前运行的就绪度和历史事件。
- 报幕执行者：在运行满足发布规则时生成演出提示摘要。

## 核心实体

- 排练运行：一次演出的 cue 集合、阶段和修订序号。
- 提示：与部门、触发点、时长和关键等级关联的演出动作。
- 复核记录：部门负责人对指定提示作出的结论和说明。
- 发布摘要：已锁定运行的部门覆盖、就绪度和摘要文本。

## 业务流程

### assemble-run

舞台监督通过 `cuebook compose` 输入节目名和导演。系统校验标识、加载真实的基础提示、规划部门执行顺序并建立处于 review 阶段的运行。成功时输出已创建的运行和提示数量；空节目名或无有效提示时返回可识别错误；通过 compose 命令验证。

### review-cue

部门负责人通过 `cuebook review` 输入节目名和部门。系统寻找该部门首个待处理提示、写入一次接受复核、刷新运行修订并记录审计事件。成功时输出已接受的部门；未知部门或已锁定运行时返回可识别错误；通过 review 命令验证。

### publish-book

报幕执行者通过 `cuebook publish` 输入节目名。系统计算跨部门就绪度，拒绝仍存在阻塞提示的运行；当所有关键部门覆盖完成时锁定运行、形成部门摘要并输出发布结果。成功时输出已发布；未完成复核时返回可识别错误；通过 publish 命令验证。

## 状态与规则

运行阶段依次为 draft、review、ready、published。每条关键提示必须拥有同部门的接受复核；拒绝复核会使运行回到 review。发布后不能再记录复核。提示编号在一次运行内唯一，部门顺序固定为灯光、音响、舞台、投影，发布摘要必须包含每个部门的覆盖率。

## 接口与验证

公开入口是 `cuebook compose`、`cuebook review`、`cuebook publish` 与 `cuebook inspect`。compose 验证建模和规划，review 验证部门决策与审计记录，publish 验证就绪度规则和发布摘要。公开 Go 测试覆盖运行组装、复核与发布边界，运行清单逐项运行三个工作流。
